import { Outfit, Rubik } from "next/font/google";

export const rubik = Rubik({
  subsets: ["latin"],
  display: "swap",
});

export const outfit = Outfit({
  subsets: ["latin"],
  display: "swap",
});
